import { CreatePageArgs } from 'gatsby';
import { PluginOptions } from './pluginOptions';

export const onCreatePage = ({ page, actions }: CreatePageArgs, options: PluginOptions) => {
  // TODO: Implement plugin onCreatePage here
};
